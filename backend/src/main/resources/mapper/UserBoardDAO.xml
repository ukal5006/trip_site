<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.trip.board.dao.UserBoardDAO">

	<resultMap type="UserBoardDTO" id="boardInfo">
		<result column="post_id" property="postId" />
		<result column="post_type" property="postType" />
		<result column="title" property="title" />
		<result column="content" property="content" />
		<result column="write_date" property="writeDate" />
		<result column="update_date" property="updateDate" />
		<result column="user_id" property="userId" />
		<result column="read_count" property="readCount" />
		<result column="good" property="good" />
		<result column="bad" property="bad" />
	</resultMap>

	<select id="getList" resultMap="boardInfo">
		SELECT *
		FROM userboard
		order by
		write_date desc
	</select>
	<select id="getListOrderDate" resultMap="boardInfo">
		SELECT *
		FROM userboard
		order by write_date desc limit 5
	</select>
	<select id="getListOrderRead" resultMap="boardInfo">
		SELECT *
		FROM userboard
		order by read_count desc limit 5
	</select>
	<select id="getListOrderGood" resultMap="boardInfo">
		SELECT *
		FROM userboard
		order by good desc limit 5
	</select>



	<select id="showContent" resultMap="boardInfo"
		parameterType="int">
		SELECT *
		FROM userboard WHERE
		post_id = #{postId}
	</select>
	
	<select id="isUser" resultType="int" parameterType="UserBoardDTO">
		select
		count(*)
		FROM userboard
		where
		post_id = #{postId} AND user_id
		=#{userId}
	</select>

	<insert id="writeBoard" parameterType="UserBoardDTO">
		INSERT INTO userboard
		(post_type, title, content, user_id, write_date)
		VALUES
		(#{postType}, #{title},
		#{content}, #{userId}, NOW())
	</insert>

	<update id="updateBoard" parameterType="UserBoardDTO">
		UPDATE userboard
		SET
		title
		= #{title}, content = #{content}, update_date = NOW()
		WHERE post_id =
		#{postId}
	</update>

	<update id="incrementViewCount" parameterType="int">
		UPDATE userboard
		SET read_count = read_count + 1
		WHERE post_id = #{postId}
	</update>

	<delete id="deleteBoard" parameterType="UserBoardDTO">
		DELETE FROM userboard
		WHERE
		post_id = #{postId}
	</delete>

	<update id="incrementGood" parameterType="int">
		UPDATE userboard
		SET
		good = good + 1
		WHERE post_id = #{postId}
	</update>

	<update id="incrementBad" parameterType="int">
		UPDATE userboard
		SET bad
		= bad + 1
		WHERE post_id = #{postId}
	</update>

	<update id="decrementGood" parameterType="int">
		UPDATE userboard
		SET
		good = good - 1
		WHERE post_id = #{postId}
	</update>

	<update id="decrementBad" parameterType="int">
		UPDATE userboard
		SET bad
		= bad - 1
		WHERE post_id = #{postId}
	</update>
</mapper>
