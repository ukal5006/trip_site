<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.trip.user.dao.UserDAO">
	<resultMap type="UserDTO" id="userInfo">
		<result column="user_id" property="userId" />
		<result column="user_password" property="userPwd" />
		<result column="birth" property="birth" />
		<result column="user_name" property="userName" />
		<result column="user_phone" property="userPhone" />
		<result column="user_email" property="userEmail" />
		<result column="admin" property="admin" />
	</resultMap>

	<resultMap type="LoginDTO" id="loginInfo">
		<result column="user_id" property="userId" />
		<result column="user_password" property="userPwd" />
	</resultMap>


	<select id="login" parameterType="LoginDTO"
		resultMap="loginInfo">
		select *
		from
		user
		where
		user_id=#{userId} and
		user_password=#{userPwd}
	</select>


	<select id="getUser" parameterType="string" resultMap="userInfo">
		SELECT *
		FROM user WHERE
		user_id =
		#{userId}
	</select>

	<insert id="insertUser" parameterType="UserDTO">
		INSERT INTO user (user_id,
		user_password, birth,
		user_name,
		user_phone, user_email, admin)
		VALUES
		(#{userId},
		#{userPwd}, #{birth},
		#{userName},
		#{userPhone},
		#{userEmail},
		#{admin})
	</insert>

	<select id="checkId" parameterType="string" resultType="int">
		SELECT
		COUNT(*)
		FROM user
		WHERE user_id=#{userId}
	</select>
	<select id="isAdmin" parameterType="string" resultType="int">
		SELECT
		admin
		FROM user
		WHERE user_id=#{userId}
	</select>

	<select id="findId" parameterType="UserDTO" resultType="string">
		SELECT
		user_id FROM user WHERE user_name = #{userName} AND birth = #{birth}
		AND user_phone = #{userPhone} AND user_email = #{userEmail}
	</select>

	<select id="findPw" parameterType="UserDTO" resultType="string">
		SELECT
		user_password FROM user WHERE user_id = #{userId} AND user_name =
		#{userName} AND birth = #{birth}
		AND user_phone = #{userPhone} AND
		user_email = #{userEmail}
	</select>

	<update id="initPw" parameterType="UserDTO">
		update user set
		user_password =
		#{userPwd} where user_id = #{userId}
	</update>

	<update id="updateUser" parameterType="UserDTO">
		update user set
		user_id =
		#{userId},
		user_password = #{userPwd},
		user_phone = #{userPhone}
		where
		user_id = #{userId}
	</update>

	<delete id="deleteUser" parameterType="String">
		delete from user
		where
		user_id = #{userId}
	</delete>


</mapper>