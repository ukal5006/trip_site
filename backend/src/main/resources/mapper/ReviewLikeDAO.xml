<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.trip.board.dao.ReviewLikeDAO">

	<resultMap type="ReviewLikeDTO" id="reviewLikeInfo">
		<result column="like_id" property="likeId" />
		<result column="review_id" property="reviewId" />
		<result column="user_id" property="userId" />
		<result column="is_good" property="isGood" />
	</resultMap>

	<insert id="insertLike" parameterType="ReviewLikeDTO">
		INSERT INTO review_like (review_id, user_id, is_good)
		VALUES (#{reviewId}, #{userId}, #{isGood})
	</insert>

	<update id="updateLike" parameterType="ReviewLikeDTO">
		UPDATE review_like
		SET is_good = #{isGood}
		WHERE review_id = #{reviewId} AND user_id = #{userId}
	</update>

	<select id="getLike" parameterType="map"
		resultMap="reviewLikeInfo">
		SELECT * FROM review_like
		WHERE review_id = #{reviewId} AND user_id = #{userId}
	</select>

	<select id="countLikes" parameterType="int" resultType="int">
		SELECT COUNT(*) FROM review_like
		WHERE review_id = #{reviewId} AND is_good = TRUE
	</select>

	<select id="countDislikes" parameterType="int" resultType="int">
		SELECT COUNT(*) FROM review_like
		WHERE review_id = #{reviewId} AND is_good = FALSE
	</select>

</mapper>
