<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.trip.board.dao.UserBoardLikeDAO">

	<resultMap type="UserBoardLikeDTO" id="userBoardLikeInfo">
		<result column="like_id" property="likeId" />
		<result column="post_id" property="postId" />
		<result column="user_id" property="userId" />
		<result column="is_good" property="isGood" />
	</resultMap>

	<insert id="insertLike" parameterType="UserBoardLikeDTO">
		INSERT INTO userboard_like
		(post_id, user_id, is_good)
		VALUES (#{postId}, #{userId}, #{isGood})
	</insert>

	<update id="updateLike" parameterType="UserBoardLikeDTO">
		UPDATE userboard_like
		SET
		is_good = #{isGood}
		WHERE post_id = #{postId} AND user_id = #{userId}
	</update>

	<select id="getLike" parameterType="map"
		resultMap="userBoardLikeInfo">
		SELECT * FROM userboard_like
		WHERE post_id = #{postId} AND
		user_id = #{userId}
	</select>

	<select id="countLikes" parameterType="int" resultType="int">
		SELECT
		COUNT(*) FROM userboard_like
		WHERE post_id = #{postId} AND is_good =
		TRUE
	</select>

	<select id="countDislikes" parameterType="int" resultType="int">
		SELECT COUNT(*) FROM userboard_like
		WHERE post_id = #{postId} AND
		is_good = FALSE
	</select>

</mapper>
